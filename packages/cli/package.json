{
  "name": "@testplane/cli",
  "version": "0.2.1",
  "description": "Daemon-backed CLI for Testplane browser-automation tools",
  "main": "build/cli.js",
  "type": "module",
  "bin": {
    "testplane-cli": "build/cli.js"
  },
  "files": [
    "build"
  ],
  "scripts": {
    "prebuild": "npm --prefix ../tools run build",
    "build": "node scripts/build.mjs",
    "typecheck": "tsc -p tsconfig.test.json --noEmit",
    "start": "node build/cli.js",
    "dev": "npm run build && node build/cli.js",
    "lint": "eslint .",
    "test-unit": "vitest run",
    "test": "npm run lint && npm run typecheck && npm run test-unit",
    "prepack": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gemini-testing/testplane-ai.git",
    "directory": "packages/cli"
  },
  "dependencies": {
    "@rrweb/replay": "^2.0.0-alpha.18",
    "@testplane/testing-library": "^1.0.5",
    "commander": "^14.0.3",
    "debug": "^4.3.4",
    "html-reporter": "11.11.0",
    "testplane": "9.0.9",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@testplane/tools": "1.0.0",
    "@types/debug": "^4.1.12",
    "@types/serve-handler": "^6.1.4",
    "esbuild": "^0.24.0",
    "serve-handler": "^6.1.6"
  }
}
